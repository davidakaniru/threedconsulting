import type { NextRequest } from "next/server";import { ValidationError } from "yup";import { ApiError } from "@/lib/api/errors";import { apiError,apiSuccess } from "@/lib/api/responses";import { requireApiAdmin } from "@/lib/auth/guards";import { cohortSchema } from "@/modules/cohorts/schemas";import { createCohort,getCohorts } from "@/modules/cohorts/server";export const runtime="nodejs";function validation(e:ValidationError){const details=e.inner.reduce<Record<string,string>>((a,i)=>{if(i.path&&!a[i.path])a[i.path]=i.message;return a},{});return apiError("VALIDATION_ERROR","Please correct the highlighted fields.",422,details)}export async function GET(r:NextRequest){try{await requireApiAdmin();const q=r.nextUrl.searchParams;return apiSuccess(await getCohorts({page:Number(q.get("page")||1),pageSize:Number(q.get("pageSize")||10),search:q.get("search")??undefined,status:q.get("status")??undefined,programmeId:q.get("programmeId")??undefined,teacherId:q.get("teacherId")??undefined}));}catch(e){if(e instanceof ApiError)return apiError(e.code,e.message,e.status,e.details);console.error(e);return apiError("INTERNAL_SERVER_ERROR","Unable to load cohorts.",500)}}export async function POST(r:NextRequest){try{const admin=await requireApiAdmin();const input=await cohortSchema.validate(await r.json(),{abortEarly:false,stripUnknown:true});return apiSuccess(await createCohort(input,admin.id),201)}catch(e){if(e instanceof ValidationError)return validation(e);if(e instanceof ApiError)return apiError(e.code,e.message,e.status,e.details);console.error(e);return apiError("INTERNAL_SERVER_ERROR","Unable to create the cohort.",500)}}
