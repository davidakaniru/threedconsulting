import type{NextRequest}from"next/server";import{ApiError}from"@/lib/api/errors";import{apiError,apiSuccess}from"@/lib/api/responses";import{requireApiAdmin}from"@/lib/auth/guards";import{performParentAction}from"@/modules/parents/server";export const runtime="nodejs";export async function POST(r:NextRequest,{params}:{params:Promise<{id:string}>}){try{const user=await requireApiAdmin();const action=await r.json();return apiSuccess(await performParentAction((await params).id,action,r.nextUrl.origin,user.id));}catch(e){if(e instanceof ApiError)return apiError(e.code,e.message,e.status,e.details);return apiError("INTERNAL_SERVER_ERROR","Unable to update parent account.",500)}}
