import type { NextRequest } from "next/server";import { ValidationError } from "yup";import { ApiError } from "@/lib/api/errors";import { apiError,apiSuccess } from "@/lib/api/responses";import { requireApiAdmin } from "@/lib/auth/guards";import { updateProgrammeSchema } from "@/modules/programmes/schemas";import { getProgramme,updateProgramme,deleteProgramme } from "@/modules/programmes/server";export const runtime="nodejs";type Context={params:Promise<{id:string}>};
export async function GET(_:NextRequest,c:Context){try{await requireApiAdmin();return apiSuccess(await getProgramme((await c.params).id));}catch(error){if(error instanceof ApiError)return apiError(error.code,error.message,error.status,error.details);return apiError("INTERNAL_SERVER_ERROR","Unable to load the programme.",500);}}
export async function PATCH(request:NextRequest,c:Context){try{const admin=await requireApiAdmin();const input=await updateProgrammeSchema.validate(await request.json(),{abortEarly:false,stripUnknown:true});return apiSuccess(await updateProgramme((await c.params).id,input,admin.id));}catch(error){if(error instanceof ValidationError){const details=error.inner.reduce<Record<string,string>>((a,i)=>{if(i.path&&!a[i.path])a[i.path]=i.message;return a;},{});return apiError("VALIDATION_ERROR","Please correct the highlighted fields.",422,details);}if(error instanceof ApiError)return apiError(error.code,error.message,error.status,error.details);return apiError("INTERNAL_SERVER_ERROR","Unable to update the programme.",500);}}

export async function DELETE(_:NextRequest,c:Context){try{const admin=await requireApiAdmin();return apiSuccess(await deleteProgramme((await c.params).id,admin.id));}catch(error){if(error instanceof ApiError)return apiError(error.code,error.message,error.status,error.details);return apiError("INTERNAL_SERVER_ERROR","Unable to delete the subject.",500);}}
