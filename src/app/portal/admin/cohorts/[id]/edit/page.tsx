import type { Metadata } from "next";import { AdminPage,PageHeader,SectionCard } from "@/components/admin/ui";import { CohortForm } from "@/modules/cohorts";import { getCohort } from "@/modules/cohorts/server";import { getTeachingAssignments } from "@/modules/teaching-assignments/server";export const metadata:Metadata={title:"Edit Cohort | Admin Portal"};export default async function Page({params}:{params:Promise<{id:string}>}){const id=(await params).id;const[c,a]=await Promise.all([getCohort(id),getTeachingAssignments({status:"active"})]);return <AdminPage><PageHeader eyebrow="Cohorts" title={`Edit ${c.name}`} description="Update the cohort configuration and lifecycle."/><SectionCard contentClassName="p-6"><CohortForm cohort={c} assignments={a.assignments}/></SectionCard></AdminPage>}
