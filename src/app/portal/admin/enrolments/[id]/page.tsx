import type {Metadata} from "next";import {AdminPage,PageBackButton,PageHeader} from "@/components/admin/ui";import {EnrolmentDetails} from "@/modules/enrolments";import {getEnrolment} from "@/modules/enrolments/server";export const metadata:Metadata={title:"Enrolment Review | Admin Portal"};export default async function Page({params}:{params:Promise<{id:string}>}){const a=await getEnrolment((await params).id);return <AdminPage><PageBackButton/><PageHeader eyebrow="Enrolments" title={a.childName} description={`Application from ${a.parentName}`}/><EnrolmentDetails application={a}/></AdminPage>}
