import type { Metadata } from "next";import { AdminPage,PageHeader } from "@/components/admin/ui";import { ProgrammeDetails } from "@/modules/programmes";import { getProgramme } from "@/modules/programmes/server";type Props={params:Promise<{id:string}>};export const metadata:Metadata={title:"Programme Details | Admin Portal"};export default async function Page({params}:Props){const programme=await getProgramme((await params).id);return <AdminPage><PageHeader eyebrow="Programmes" title={programme.name} description="Review this programme and its future academic relationships."/><ProgrammeDetails programme={programme}/></AdminPage>}
