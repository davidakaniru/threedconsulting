import type{Metadata}from"next";import{notFound}from"next/navigation";import{AdminPage,PageBackButton,PageHeader}from"@/components/admin/ui";import{requireTeacher}from"@/lib/auth/guards";import{getHomework}from"@/modules/homework/server";import{HomeworkDetails}from"@/modules/homework";export const metadata:Metadata={title:"Homework Details | Teacher Portal"};export default async function Page({params}:{params:Promise<{id:string}>}){const teacher=await requireTeacher();const h=await getHomework((await params).id);if(h.session.lesson.teacher.id!==teacher.id)notFound();return <AdminPage><PageBackButton/><PageHeader eyebrow="Homework" title={h.title} description={`${h.session.lesson.student.name} · ${h.session.title}`}/><HomeworkDetails homework={h}/></AdminPage>}
