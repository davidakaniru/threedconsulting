import type{Metadata}from"next";import{AdminPage,PageBackButton,PageHeader,SectionCard}from"@/components/admin/ui";import{requireTeacher}from"@/lib/auth/guards";import{getCohorts}from"@/modules/cohorts/server";import{SessionForm}from"@/modules/sessions";export const metadata:Metadata={title:"New Session | Teacher Portal"};export default async function Page(){const teacher=await requireTeacher();const{cohorts}=await getCohorts({teacherId:teacher.id,pageSize:100});const availableCohorts=cohorts.filter(c=>c.status==="open"||c.status==="active");return <AdminPage><PageBackButton/><PageHeader eyebrow="Sessions" title="Create session" description="Schedule a strictly online class session for one of your cohorts."/><SectionCard contentClassName="p-6"><SessionForm cohorts={availableCohorts}/></SectionCard></AdminPage>}
