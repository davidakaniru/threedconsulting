"use client";import Link from"next/link";import{useSearchParams}from"next/navigation";import{BookOpen,Plus}from"lucide-react";import{Button}from"@/components/ui/button";import{EmptyState,SectionCard,StatusBadge}from"@/components/admin/ui";import{useTeacherHomework}from"../hooks";
export function HomeworkList(){const params=useSearchParams();const sessionId=params.get("sessionId")??undefined;const q=useTeacherHomework({pageSize:100,sessionId});if(q.isLoading)return <SectionCard contentClassName="p-6">Loading homework…</SectionCard>;if(q.isError)return <SectionCard contentClassName="p-6">Homework could not be loaded.</SectionCard>;const items=q.data?.homework??[];return items.length===0?<EmptyState icon={BookOpen} title="No homework yet" description="Create homework for one of your sessions." action={<Button asChild><Link href="/portal/teacher/homework/new"><Plus/>New homework</Link></Button>}/>:<SectionCard contentClassName="p-0"><div className="divide-y divide-slate-100">{items.map(h=><Link key={h.id} href={`/portal/teacher/homework/${h.id}`} className="flex items-center justify-between gap-4 px-6 py-4 hover:bg-slate-50"><div><p className="font-bold">{h.title}</p><p className="mt-1 text-sm text-slate-500">{h.session.cohort.code} · {h.session.title} · Due {new Date(h.dueAt).toLocaleString("en-GB",{dateStyle:"medium",timeStyle:"short"})}</p></div><div className="flex items-center gap-3"><StatusBadge status={h.status}/><span className="text-xs font-semibold text-slate-500">{h.submissions.total} learners</span></div></Link>)}</div></SectionCard>}
