"use client";import Link from"next/link";import{Mail,Pencil,Phone,Briefcase,Users,ShieldCheck,Send}from"lucide-react";import{Button}from"@/components/ui/button";import{SectionCard,StatusBadge,InfoCard}from"@/components/admin/ui";import{useParentAction}from"@/modules/parents/hooks";import type{ParentDetail}from"@/modules/parents/types";import{toast}from"sonner";import{toApiError}from"@/lib/api/errors";export function ParentDetails({parent}:{parent:ParentDetail}){const action=useParentAction(parent.id);async function run(v:any){try{await action.mutateAsync(v);toast.success("Parent account updated.");}catch(e){toast.error(toApiError(e).message)}}return <div className="grid gap-6 xl:grid-cols-[1fr_320px]"><div className="space-y-6"><SectionCard contentClassName="p-6"><div className="flex justify-between gap-4"><div><h2 className="font-display text-2xl font-extrabold">{parent.firstName} {parent.lastName}</h2><p className="text-sm text-muted-foreground">{parent.email}</p><div className="mt-3 flex gap-2"><StatusBadge status={parent.onboardingStatus}/><StatusBadge status={parent.accountStatus}/></div></div><Button asChild variant="outline"><Link href={`/portal/admin/parents/${parent.id}/edit`}><Pencil/>Edit</Link></Button></div></SectionCard><SectionCard title="Contact information" contentClassName="grid gap-4 p-6 sm:grid-cols-2"><InfoCard icon={Mail} title="Email" description={parent.email}/><InfoCard icon={Phone} title="Phone" description={parent.phone||"Not provided"}/><InfoCard icon={Briefcase} title="Occupation" description={parent.occupation||"Not provided"}/><InfoCard icon={ShieldCheck} title="Address" description={parent.address||"Not provided"}/></SectionCard><SectionCard title="Linked students" description="Children connected to this parent account." contentClassName="space-y-3 p-6">{parent.students.length?parent.students.map(s=><Link key={s.id} href={`/portal/admin/students/${s.id}`} className="flex justify-between rounded-xl border p-4 hover:bg-muted"><div><p className="font-bold">{[s.firstName,s.middleName,s.lastName].filter(Boolean).join(" ")}</p><p className="text-xs text-muted-foreground">{s.admissionNumber}</p></div><div className="text-right text-sm capitalize"><p>{s.relationship.replaceAll("_"," ")}</p>{s.isPrimaryContact&&<p className="text-xs text-primary">Primary contact</p>}</div></Link>):<p className="text-sm text-muted-foreground">No students linked.</p>}</SectionCard></div><aside className="space-y-4"><InfoCard icon={Users} title="Students" description={`${parent.studentsCount} linked student${parent.studentsCount===1?"":"s"}.`}/>{parent.onboardingStatus==="invited"&&<Button className="w-full" onClick={()=>void run({type:"resend_invitation"})} disabled={action.isPending}><Send/>Resend invitation</Button>}<Button variant={parent.accountStatus==="suspended"?"default":"destructive"} className="w-full" onClick={()=>void run({type:"account_status",status:parent.accountStatus==="suspended"?"active":"suspended"})}>{parent.accountStatus==="suspended"?"Reactivate account":"Suspend account"}</Button></aside></div>}
