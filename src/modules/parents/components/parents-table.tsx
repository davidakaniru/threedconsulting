"use client";import Link from"next/link";import{useDeferredValue,useMemo,useState}from"react";import{Eye,Pencil,Plus,Users}from"lucide-react";import{Button}from"@/components/ui/button";import{DataTable,EmptyState,FilterSelect,Pagination,RowActions,SearchInput,SectionCard,StatusBadge,TableError,TableLoading,TableToolbar,type DataTableColumn}from"@/components/admin/ui";import{useParents}from"@/modules/parents/hooks";import type{ParentSummary}from"@/modules/parents/types";const opts=[{label:"All statuses",value:"all"},{label:"Invited",value:"invited"},{label:"Active",value:"active"}];export function ParentsTable(){const[s,setS]=useState("");const[status,setStatus]=useState("all");const[p,setP]=useState(1);const d=useDeferredValue(s);const q=useParents({page:p,pageSize:10,search:d||undefined,status});const cols=useMemo<DataTableColumn<ParentSummary>[]>(()=>[{id:"parent",header:"Parent",cell:x=><div><p className="font-extrabold">{x.firstName} {x.lastName}</p><p className="text-xs text-slate-500">{x.email}</p></div>},{id:"students",header:"Students",cell:x=>x.studentsCount},{id:"onboarding",header:"Onboarding",cell:x=><StatusBadge status={x.onboardingStatus}/>},{id:"account",header:"Account",cell:x=><StatusBadge status={x.accountStatus}/>},{id:"actions",header:"",cell:x=><RowActions label="Parent actions" actions={[{label:"View parent",icon:Eye,href:`/portal/admin/parents/${x.id}`},{label:"Edit parent",icon:Pencil,href:`/portal/admin/parents/${x.id}/edit`}]} />}],[]);const pages=Math.max(1,Math.ceil((q.data?.total??0)/10));return <SectionCard className="overflow-hidden"><TableToolbar search={<SearchInput id="parent-search" value={s} onChange={v=>{setS(v);setP(1)}} placeholder="Search parents..."/>} filters={<FilterSelect id="parent-status" value={status} onValueChange={v=>{setStatus(v);setP(1)}} options={opts}/>} actions={<Button asChild className="sm:hidden"><Link href="/portal/admin/parents/new"><Plus/>Add parent</Link></Button>}/>{q.isLoading?<TableLoading/>:q.isError?<TableError title="Parents could not be loaded" description="Try again." onRetry={()=>void q.refetch()}/>:<DataTable data={q.data?.parents??[]} columns={cols} getRowId={x=>x.id} emptyState={<EmptyState icon={Users} title="No parents found" description="Invite the first parent account."/>}/>} {q.data&&q.data.total>0&&<Pagination page={p} totalPages={pages} total={q.data.total} label="parents" onPageChange={setP}/>}</SectionCard>}
