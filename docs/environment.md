# Environment Variables

Keep secrets in `.env.local` and never commit them. Core values include Supabase URL, anon key, service-role key, and optional Resend transactional email settings. Production values must be configured in the deployment platform.
