# Architecture

The application uses Next.js App Router, Supabase, React Query, React Hook Form, and Yup. Domain modules are organized under `src/modules`, with repository, mapper, service, API, hooks, and UI layers where appropriate. Role guards protect Admin, Teacher, and Parent routes.
