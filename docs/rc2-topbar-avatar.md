# RC-2 follow-up — Portal top-bar avatar

The portal top-bar profile button now renders the authenticated user's `avatarUrl` when available. If no profile image exists, it falls back to initials capped at two characters. Existing menu/dropdown behaviour is unchanged.
