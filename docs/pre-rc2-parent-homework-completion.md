# Parent homework completion

Parents can mark published homework for their linked child as done from the Parent Portal. The action updates the existing `homework_submissions` record from `pending`/`late` to `submitted` and records `submitted_at`. Submitted or graded homework is no longer counted or styled as overdue.
