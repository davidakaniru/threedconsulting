# Coding Standards

- Use React Hook Form and Yup for forms.
- Reuse shared fields and admin UI components.
- Keep domain rules in services or transactional database functions.
- Use React Query keys and invalidation consistently.
- Preserve role boundaries and server-side authorization.
- Use Parent Portal child terminology while retaining Student in code and data models.
